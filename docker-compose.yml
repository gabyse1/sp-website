version: '3.9'

services:
  app:
    build:
      context: .
    ports:
      - 8000:8000
    volumes:
      - ./app:/app
    env_file:
      - ./.env/.env.app
    depends_on:
      - db

  db:
    image: postgres:14-alpine
    env_file:
      - ./.env/.env.db